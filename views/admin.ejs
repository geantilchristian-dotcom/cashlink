<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TERMINAL DE CONTRÔLE | CASHLINK ELITE v3.0</title>
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>

    <style>
        :root {
            --bg-master: #000c24;
            --card-glass: rgba(255, 255, 255, 0.03);
            --border-glass: rgba(255, 255, 255, 0.08);
            --neon-blue: #00d9ff;
            --neon-purple: #bc13fe;
            --emerald: #10b981;
            --rose: #f43f5e;
            --gold: #ffb400;
            --sidebar-width: 290px;
        }

        body {
            background-color: var(--bg-master);
            background-image: radial-gradient(circle at 50% -20%, #001a4d, transparent);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: #ffffff; margin: 0; overflow-x: hidden;
        }

        /* --- SIDEBAR --- */
        .admin-sidebar {
            width: var(--sidebar-width); height: 100vh;
            background: rgba(0, 0, 0, 0.8); backdrop-filter: blur(30px);
            position: fixed; left: 0; top: 0; padding: 40px 20px;
            border-right: 1px solid var(--border-glass); z-index: 1000;
        }

        .nav-item-custom {
            display: flex; align-items: center; gap: 15px; padding: 15px 22px;
            color: rgba(255,255,255,0.4); text-decoration: none; border-radius: 20px;
            margin-bottom: 10px; font-weight: 700; cursor: pointer; transition: 0.3s;
        }

        .nav-item-custom:hover { background: rgba(255,255,255,0.05); color: #fff; }
        .nav-item-custom.active { background: var(--neon-blue); color: #000; box-shadow: 0 0 25px rgba(0, 217, 255, 0.3); }

        /* --- CONTENU --- */
        .admin-main-content { margin-left: var(--sidebar-width); padding: 50px; }

        .glass-card {
            background: var(--card-glass); border: 1px solid var(--border-glass);
            border-radius: 40px; padding: 40px; margin-bottom: 40px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.4);
        }

        .stat-val { font-size: 2.2rem; font-weight: 800; color: var(--neon-blue); letter-spacing: -1px; }
        .stat-label { font-size: 0.75rem; text-transform: uppercase; font-weight: 800; opacity: 0.5; letter-spacing: 1px; }

        /* --- FORMULAIRES --- */
        .admin-label { font-size: 0.7rem; font-weight: 800; color: var(--neon-blue); text-transform: uppercase; margin-bottom: 8px; display: block; }
        .admin-input {
            background: rgba(0, 0, 0, 0.4); border: 1px solid var(--border-glass);
            border-radius: 18px; padding: 15px 20px; color: white; width: 100%; margin-bottom: 20px;
            font-weight: 600; transition: 0.3s;
        }
        .admin-input:focus { border-color: var(--neon-blue); outline: none; box-shadow: 0 0 15px rgba(0, 217, 255, 0.15); }

        .btn-update { background: var(--neon-blue); color: #000; border: none; padding: 18px; border-radius: 20px; font-weight: 800; width: 100%; text-transform: uppercase; letter-spacing: 1px; transition: 0.3s; cursor: pointer; }
        .btn-update:hover { transform: translateY(-3px); box-shadow: 0 10px 25px rgba(0, 217, 255, 0.4); }

        .section-panel { display: none; }
        .section-panel.active { display: block; animation: fadeInUp 0.5s ease-out; }

        @keyframes fadeInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

        /* --- TABLES --- */
        .premium-table { width: 100%; border-collapse: separate; border-spacing: 0 12px; }
        .premium-table td { background: rgba(255,255,255,0.02); padding: 20px; border: none; font-size: 0.95rem; font-weight: 600; }
        .premium-table tr td:first-child { border-radius: 20px 0 0 20px; }
        .premium-table tr td:last-child { border-radius: 0 20px 20px 0; }
    </style>
</head>
<body>

    <div class="admin-sidebar">
        <div class="text-center mb-5">
            <div class="p-3 mb-3 animate__animated animate__pulse animate__infinite" style="background: var(--neon-blue); border-radius: 22px; display: inline-block;">
                <i class="bi bi-cpu-fill text-dark fs-3"></i>
            </div>
            <h5 class="fw-900 tracking-tighter">ELITE COMMAND</h5>
            <span class="badge bg-danger rounded-pill" style="font-size: 0.6rem;">TERMINAL RDC 2026</span>
        </div>

        <div class="nav-menu">
            <div class="nav-item-custom active" onclick="openTab('stats', this)"><i class="bi bi-graph-up"></i> Performances</div>
            <div class="nav-item-custom" onclick="openTab('validations', this)"><i class="bi bi-check2-circle"></i> Validations TID <% if(stats.enAttente > 0){ %><span class="badge bg-danger ms-auto"><%= stats.enAttente %></span><% } %></div>
            <div class="nav-item-custom" onclick="openTab('interface', this)"><i class="bi bi-pencil-square"></i> Modifier Dashboard</div>
            <div class="nav-item-custom" onclick="openTab('members', this)"><i class="bi bi-people"></i> Investisseurs</div>
            <div class="nav-item-custom" onclick="openTab('logs', this)"><i class="bi bi-list-columns"></i> Historique</div>
        </div>

        <div style="margin-top: auto;">
            <a href="/" class="nav-item-custom text-info"><i class="bi bi-arrow-left"></i> VOIR LE SITE</a>
        </div>
    </div>

    <div class="admin-main-content">

        <div id="stats" class="section-panel active">
            <h3 class="fw-800 mb-5 text-uppercase">Statistiques Système</h3>
            <div class="row g-4 mb-4">
                <div class="col-md-4">
                    <div class="glass-card text-center">
                        <span class="stat-label">Total Membres</span>
                        <div class="stat-val"><%= stats.totalUsers %></div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="glass-card text-center">
                        <span class="stat-label">Transactions en Attente</span>
                        <div class="stat-val text-warning"><%= stats.enAttente %></div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="glass-card text-center">
                        <span class="stat-label">Dette Globale</span>
                        <div class="stat-val text-info"><%= stats.soldeTotalUsers.toLocaleString() %> FC</div>
                    </div>
                </div>
            </div>
        </div>

        <div id="validations" class="section-panel">
            <div class="glass-card">
                <h4 class="fw-900 text-info mb-4">Flux de Paiements</h4>
                <div class="table-responsive">
                    <table class="premium-table">
                        <thead>
                            <tr>
                                <th class="admin-label">Opération</th>
                                <th class="admin-label">Référence</th>
                                <th class="admin-label">Montant</th>
                                <th class="admin-label">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% transactions.filter(t => t.statut === 'En attente').forEach(tx => { %>
                            <tr>
                                <td><span class="badge bg-primary"><%= tx.type %></span></td>
                                <td><code class="text-white"><%= tx.reference %></code></td>
                                <td class="fw-bold"><%= tx.montant.toLocaleString() %> FC</td>
                                <td>
                                    <form action="/valider-depot" method="POST">
                                        <input type="hidden" name="txId" value="<%= tx._id %>">
                                        <button class="btn btn-success btn-sm rounded-pill px-3">APPROUVER</button>
                                    </form>
                                </td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div id="interface" class="section-panel">
            <div class="glass-card">
                <h4 class="fw-900 text-info mb-4">Configuration du Dashboard</h4>
                <form action="/admin/update-content" method="POST">
                    <div class="row">
                        <div class="col-md-6">
                            <label class="admin-label">Étiquette Tech (Haut)</label>
                            <input type="text" name="tag_tech" class="admin-input" value="<%= site.tag_tech %>">

                            <label class="admin-label">Titre Principal</label>
                            <input type="text" name="titre_principal" class="admin-input" value="<%= site.titre_principal %>">

                            <label class="admin-label">Puissance de Minage (Hash)</label>
                            <input type="text" name="puissance_hash" class="admin-input" value="<%= site.puissance_hash %>">
                            
                            <label class="admin-label">Réseau Status</label>
                            <input type="text" name="reseau_status" class="admin-input" value="<%= site.reseau_status %>">
                        </div>
                        <div class="col-md-6">
                            <label class="admin-label">Description / Slogan</label>
                            <textarea name="description" class="admin-input" rows="3"><%= site.description %></textarea>

                            <label class="admin-label">Sécurité / Protocole</label>
                            <input type="text" name="securite_type" class="admin-input" value="<%= site.securite_type %>">

                            <label class="admin-label">Status Retraits</label>
                            <input type="text" name="retraits_status" class="admin-input" value="<%= site.retraits_status %>">

                            <label class="admin-label">Footer Principal</label>
                            <input type="text" name="footer_text" class="admin-input" value="<%= site.footer_text %>">
                        </div>
                    </div>
                    <button type="submit" class="btn-update mt-4">SAUVEGARDER LES MODIFICATIONS</button>
                </form>
            </div>
        </div>

        <div id="members" class="section-panel">
            <div class="glass-card">
                <h4 class="fw-900 mb-4">Base des Investisseurs</h4>
                <div class="table-responsive">
                    <table class="premium-table">
                        <thead>
                            <tr>
                                <th class="admin-label">Nom</th>
                                <th class="admin-label">Pack</th>
                                <th class="admin-label">Solde</th>
                                <th class="admin-label">Gains</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(u => { %>
                            <tr>
                                <td><%= u.username %></td>
                                <td><span class="badge bg-dark"><%= u.pack %></span></td>
                                <td class="text-info"><%= u.solde.toLocaleString() %> FC</td>
                                <td class="text-success"><%= u.bonus.toLocaleString() %> FC</td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <div id="logs" class="section-panel">
            <div class="glass-card">
                <h4 class="fw-900 mb-4">Derniers Événements</h4>
                <div class="table-responsive">
                    <table class="premium-table">
                        <tbody>
                            <% messages.forEach(log => { %>
                            <tr>
                                <td class="small opacity-50" style="width: 200px;"><%= log.date %></td>
                                <td class="fw-bold"><%= log.text %></td>
                            </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

    </div>

    <script>
        function openTab(tabId, btn) {
            document.querySelectorAll('.section-panel').forEach(s => s.classList.remove('active'));
            document.getElementById(tabId).classList.add('active');
            document.querySelectorAll('.nav-item-custom').forEach(n => n.classList.remove('active'));
            btn.classList.add('active');
        }
    </script>
</body>
</html>
